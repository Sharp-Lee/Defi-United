// P10d compatibility boundary: vault state currently lives with the browser account
// model. Later milestones should narrow this surface when vault/session code splits.
export * from "../browserVault/accounts";
