// P10d compatibility boundary: this preserves the existing browser vault account module,
// including its ethers browser-crypto registration, until the account domain is split.
export * from "../browserVault/accounts";
