# Project Status

This file records the current PWA-only milestones. For the broader current-state summary, see `docs/superpowers/project-overview.md`.

## P10 PWA mainline

| milestone | task | branch | commit | review status | verification | pushed | merged | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P10 | PWA direction and docs convergence | `main` | current | passed | passed | yes | yes | README, roadmap, workflow, and status are aligned to the browser-first PWA mainline. |
| P10a | PWA shell and installability baseline | `main` | current | passed | passed | yes | yes | The browser-first shell, Chinese navigation, and manifest baseline are in place. |
| P10b | Browser encrypted vault and account groups | `main` | current | passed | passed | yes | yes | The browser encrypted vault, hot session, account groups, and account derivation are implemented and tested. |
| P10c | Chain / RPC config and shared fee panel plus vault import hardening | `main` | `c8cb3c0` | passed | `npm test`; `npm run typecheck`; `npm run build`; `npm run smoke:browser`; `git diff --check` | yes | yes | Browser-side chain/RPC config, shared fee panel, password-verified vault import, stronger KDF policy, production/mobile smoke, and PWA icons are pushed to `main`. |
| P10d | Clean architecture rebase spec | `main` | `e164c31` | self-reviewed | `git diff --check` | yes | yes | Spec aligns the professional shell and future 985monitor-class product lanes before implementation planning. |
| P10d | Clean architecture rebase implementation plan | `codex/p10d-clean-architecture-rebase` | `c881d55` | self-reviewed | `npm test`; `npm run typecheck`; `git diff --check` | yes | yes | Plan decomposes P10d into serial architecture, shell, feature migration, style, docs, review, and merge tasks. |
| P10d | Task 1 architecture boundaries and compatibility re-exports | `codex/p10d-clean-architecture-rebase` | `91e0e4d` | spec passed; quality passed | `npm test -- src/shared/format/number.test.ts src/app/PwaShell.test.tsx`; `npm run typecheck`; `git diff --check` | yes | yes | Adds core/service/shared compatibility boundaries and shared fee cost formatter without changing send behavior. |
| P10d | Task 2 professional console shell | `codex/p10d-clean-architecture-rebase` | `ea677a9` | spec passed; quality passed | `npm test -- src/app/state/appNavigation.test.ts src/app/PwaShell.test.tsx`; `npm run typecheck`; `git diff --check` | yes | yes | Adds left navigation, top context bar, main workspace, right preview/risk rail, placeholder modules, and minimal shell styling without enabling send behavior. |
| P10d | Task 3 account feature migration | `codex/p10d-clean-architecture-rebase` | `fa18d90` | spec passed; quality passed | `npm test -- src/features/pwaVault/PwaVaultAccessView.test.tsx src/features/pwaVault/PwaVaultWorkspace.test.tsx src/app/PwaShell.test.tsx`; `npm run typecheck`; `git diff --check` | yes | yes | Moves account vault UI into `features/accounts` with old `features/pwaVault` compatibility re-exports and preserves vault behavior. |
| P10d | Task 4 settings feature migration | `codex/p10d-clean-architecture-rebase` | `827cb99` | spec passed; quality passed | `npm test -- src/lib/browserChainConfig.test.ts src/core/browserChainConfig.test.ts src/app/PwaShell.test.tsx`; `npm run typecheck`; `git diff --check` | yes | yes | Moves chain/RPC and shared fee controls into `features/settings`, keeps old `features/pwaSettings` compatibility re-exports, preserves RPC persistence, and verifies fee drafts reset on reload. |
| P10d | Task 5 split styles and browser smoke coverage | `codex/p10d-clean-architecture-rebase` | `b37a8a8` | spec passed; quality passed | `npm test -- src/app/PwaShell.test.tsx`; `npm run typecheck`; `npm run smoke:browser`; `git diff --check` | yes | yes | Splits CSS into tokens/base/layout/components/features files and updates desktop/mobile browser smoke for the P10d console shell labels and safety rails. |
| P10d | Documentation, status, and full release gate | `codex/p10d-clean-architecture-rebase` | `0e00fbf` | spec passed; quality passed; controller gate passed | `npm test`; `npm run typecheck`; `npm run build`; `npm run smoke:browser`; `git diff --check` | yes | yes | Final P10d docs mark the architecture/shell/source split complete on the milestone branch; full release gate passed before merge. |
| P10d | Final review gap fixes | `codex/p10d-clean-architecture-rebase` | `9d11931` | spec passed; quality passed after re-review | `npm test`; `npm run typecheck`; `npm run build`; `npm run smoke:browser`; `git diff --check` | yes | yes | Closes final review gaps by surfacing RPC and fee context in the top bar, updating current PWA capability docs, and clarifying encrypted-vault versus chain/RPC persistence boundaries. |
| P10d | Milestone merge to main | `main` | `19641d0` | controller post-merge verified | `npm test`; `npm run typecheck`; `npm run build`; `npm run smoke:browser`; `git diff --check` | yes | yes | No-ff merged `codex/p10d-clean-architecture-rebase` into main and reran the full PWA release gate on main. |
| P11 | Account library expansion spec | `codex/p11-account-library` | `b4408c3` | spec passed | `git diff --check` | yes | yes | Defines multi-select, batch derivation, encrypted-vault persistence, and no-send boundaries. |
| P11 | Account library implementation plan | `codex/p11-account-library` | `8ea63ba` | self-reviewed | `git diff --check` | yes | yes | Decomposes P11 into core helpers, UI wiring, smoke/docs/status, final review, and merge gates. |
| P11 | Core account library helpers | `codex/p11-account-library` | `b896621` | spec passed; quality passed | `npm test -- src/core/browserVault/accounts.test.ts`; `npm run typecheck`; `git diff --check` | yes | yes | Adds multi-select account helpers, account-library summary, selection-preserving derivation, and no-op safety tests. |
| P11 | Account library multi-select UI | `codex/p11-account-library` | `9a49897` | spec passed; quality passed after re-review | `npm test -- src/features/pwaVault/PwaVaultWorkspace.test.tsx src/app/PwaShell.test.tsx`; `npm run typecheck`; `git diff --check` | yes | yes | Adds account-library summary, default 20-account derivation, select all / clear, checkbox toggles, encrypted-vault persistence, and mobile-safe summary badges without signing or broadcast behavior. |
| P11 | Smoke, docs, and Playwright artifact hygiene | `codex/p11-account-library` | `caeed51` | final spec passed; final quality passed after re-review | `npm test`; `npm run typecheck`; `npm run build`; `npm run smoke:browser`; `git diff --check` | yes | yes | Updates P11 smoke/docs/status and ignores Playwright output directories so full release gates do not leave untracked artifacts. |
| P11 | Final review status update | `codex/p11-account-library` | `51b1fd7` | final spec passed; final quality passed | `npm test`; `npm run typecheck`; `npm run build`; `npm run smoke:browser`; `git diff --check` | yes | yes | Records final P11 branch review and verification status before milestone merge. |
| P11 | Milestone merge to main | `main` | `2615301` | controller post-merge verified | `npm test`; `npm run typecheck`; `npm run build`; `npm run smoke:browser`; `git diff --check` | yes | yes | No-ff merged `codex/p11-account-library` into main and reran the full PWA release gate on main. |
| P11-P18 | Full wallet workbench product target | `main` | `e164c31` | self-reviewed | `git diff --check` | yes | yes | Product capability spec records account, asset, queue, distribution, inscription, ABI, reverse parsing, and mobile/release requirements. |
| P12 | Asset watchlist and balance snapshots spec | `codex/p12-asset-watchlist` | `0330edb` | self-reviewed | `git diff --check` | yes | yes | Defines read-only asset visibility, chainId validation, watched ERC-20 definitions, session-only balance snapshots, stale/partial/error states, and no-send boundaries. |
| P12 | Asset watchlist implementation plan | `codex/p12-asset-watchlist` | `0da00de` | self-reviewed | `git diff --check` | yes | yes | Decomposes P12 into asset registry, balance refresh core, asset UI, shell integration/smoke, docs, and release-gate tasks. |
| P12 | Asset registry core and storage | `codex/p12-asset-watchlist` | `5f49381` | spec passed; quality passed after re-review | `npm test -- src/core/assets/browserAssetRegistry.test.ts src/lib/browserAssetRegistry.test.ts`; `npm run typecheck`; `git diff --check` | yes | yes | Adds local watched ERC-20 registry and storage with canonical persisted-state validation, duplicate protection, localStorage key coverage, and no balance/secret persistence. |
| P12 | Balance refresh core and RPC client | `codex/p12-asset-watchlist` | `ea1f5aa` | spec passed after re-review; quality passed after re-review | `npm test -- src/services/rpc/browserJsonRpcClient.test.ts src/core/assets/balanceSnapshots.test.ts`; `npm run typecheck`; `git diff --check` | yes | yes | Adds read-only JSON-RPC client, chain-validated native/ERC-20 refresh core, stale preservation, partial-failure reporting, and RPC error redaction. |
| P12 | Read-only asset workspace UI | `codex/p12-asset-watchlist` | `061650d` | spec passed; quality passed after re-review | `npm test -- src/features/assets/PwaAssetWorkspace.test.tsx`; `npm run typecheck`; `git diff --check` | yes | yes | Adds the standalone read-only asset workspace, watched token editor, selected-account balance tables, stale/partial/chain-mismatch/no-selected states, and UI-layer RPC error redaction without shell wiring or send controls. |
| P12 | Shell integration, refresh wiring, and browser smoke | `codex/p12-asset-watchlist` | `de43ab3` | spec passed after re-review; quality passed after re-review | `npm test -- src/app/PwaShell.test.tsx`; `npm run typecheck`; `npm run smoke:browser`; `git diff --check` | yes | yes | Marks the asset module ready, wires session-only chain-validated balance refresh into the PWA shell, persists only watched token definitions, blocks disabled-RPC refresh, clears/invalidate snapshots on lock/context change, and extends desktop/mobile smoke without live RPC calls. Follow-up docs status was recorded in `d59050f`. |
| P12 | Docs and current capability wording | `codex/p12-asset-watchlist` | `7ab0870` | spec passed after wording fix; quality passed | `git diff --check`; `git diff --cached --check` | yes | yes | Updates README, spec, roadmap, overview, and status wording for the P12 read-only asset branch while distinguishing shipped `main` from the milestone branch. |
| P12 | Final review gap fixes and branch release gate | `codex/p12-asset-watchlist` | `58829a9` | final spec passed after re-review; final quality passed; controller gate passed | `npm test`; `npm run typecheck`; `npm run build`; `npm run smoke:browser`; `git diff --check` | yes | yes | Closes final review gaps by requiring primary enabled RPC for asset refresh, clearing session snapshots on asset context changes, showing account/token failure context, and rerunning the full P12 branch release gate. |
| P12 | Milestone merge to main | `main` | `bddf5ee` | controller post-merge verified | `npm test`; `npm run typecheck`; `npm run build`; `npm run smoke:browser`; `git diff --check` | no | yes | No-ff merged `codex/p12-asset-watchlist` into main and reran the full PWA release gate on main. |
| P13 | Execution queue and history spec | `codex/p13-execution-queue-history` | `b289000` | spec passed after re-review | `git diff --check`; baseline `npm test` | yes | no | Defines the P13 queue/history foundation, production UI hard boundary, nonce ordering, scheduling semantics, stop/resume/retry/rerun behavior, redacted history/export, and no arbitrary send-form rule before implementation planning. |
| P13 | Execution queue and history implementation plan | `codex/p13-execution-queue-history` | `00fae90` | spec re-review passed | `git diff --check`; plan red-flag scan | yes | no | Plan decomposes P13 into queue model/redaction/history, browser storage, injected execution engine, queue workspace UI, shell/smoke integration, docs, final review, and merge gates. Review gaps closed for active session recovery, nonce-consumed retry semantics, signing-vs-broadcast failures, txHash allowance, redaction, and UI policy controls. |
| P13 | Task 1 queue types, redaction, and history model | `codex/p13-execution-queue-history` | `d8f2e5f` | spec passed after re-review; quality passed after re-review | `npm test -- src/core/queue/queueRedaction.test.ts src/core/queue/queueHistory.test.ts`; `npm run typecheck`; `git diff --check` | yes | no | Adds pure queue history types, strict redacted history validation/export, txHash allowance, websocket URL redaction, BIP39-style mnemonic redaction, safe ID/numeric field validation, and no storage/UI/execution behavior. |
| P13 | Task 2 queue history browser storage | `codex/p13-execution-queue-history` | `60e6398` | spec passed; quality passed after re-review | `npm test -- src/lib/browserQueueHistory.test.ts src/core/queue/queueHistory.test.ts src/core/queue/queueRedaction.test.ts`; `npm run typecheck`; `git diff --cached --check`; `git diff --check` | yes | no | Adds localStorage and memory adapters for validated redacted queue history, default empty loads, tampered unsafe persisted-key rejection, clear support, and no active draft or raw transaction persistence. |
| P13 | Task 3 injected queue execution engine | `codex/p13-execution-queue-history` | `fa423f1` | spec passed after re-review; quality passed after re-review | `npm test -- src/core/queue/queueExecution.test.ts src/core/queue/queueHistory.test.ts src/core/queue/queueRedaction.test.ts`; `npm run typecheck`; `git diff --cached --check`; `git diff --check` | yes | no | Adds a pure injected execution engine with per-account nonce lanes, cross-account concurrency, global RPC call-start token bucket, stop/recovery helpers, fee propagation, recovery draft matching, runtime policy validation, redacted errors, and no UI/storage/send form. |

## Next

Continue P13 on `codex/p13-execution-queue-history`. Next: execute Task 4 queue/history workspace UI with subagent-driven development.
